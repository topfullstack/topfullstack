import { CreatedByInterceptor } from './created-by.interceptor';

describe('CreatedByInterceptor', () => {
  it('should be defined', () => {
    expect(new CreatedByInterceptor()).toBeDefined();
  });
});
